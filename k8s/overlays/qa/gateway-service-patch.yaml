# LoadBalancer gives the gateway a public IP on GKE (kind/local uses
# port-forward instead). This is the only Service exposed outside the
# cluster; everything else stays ClusterIP.
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  type: LoadBalancer
