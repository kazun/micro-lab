# Placeholder — for real prod, inject via CI/CD or a secrets manager
# (Vault, cloud secrets manager, Sealed Secrets, SOPS). Never commit real
# production credentials to git.
apiVersion: v1
kind: Secret
metadata:
  name: peap-db-secret
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME_VIA_CI_CD"
  JWT_SECRET: "CHANGE_ME_VIA_CI_CD"
