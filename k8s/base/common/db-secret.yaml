# NOTE: placeholder for local/dev convenience only. Each overlay patches this
# with its own value; for qa/prod, replace with values injected by CI/CD or a
# real secrets manager (Vault, cloud secrets manager, Sealed Secrets, SOPS).
apiVersion: v1
kind: Secret
metadata:
  name: peap-db-secret
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: "changeme"
  JWT_SECRET: "dev-only-secret-change-me-please-0123456789ABCDEF"
